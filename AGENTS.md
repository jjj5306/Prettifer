# AGENTS.md

이 문서는 Prettifer 저장소에서 작업하는 사람과 AI 에이전트가 따라야 하는 기본 개발 규칙을 정의한다.

## 1. 기본 원칙

- 모든 변경은 GitHub 이슈를 기준으로 관리한다.
- GitHub 이슈는 작업의 목적, 범위, 진행 상태와 관련 링크를 사람이 읽기 쉽게 요약한다.
- OpenSpec은 상세 요구사항, 시나리오, 설계 결정과 구현 작업의 기준이다.
- 개발은 SDD(Spec-Driven Development)와 TDD(Test-Driven Development)를 기본 방식으로 사용한다.
- 코드는 가독성, 재사용성, 유지보수성을 지속적으로 고려한다.
- 사용하는 언어와 프레임워크의 기본 설계 원칙을 준수한다.
- 현재 이슈의 범위를 벗어나는 변경을 함께 포함하지 않는다.

## 2. 이슈 기반 작업

### 2.1 작업 시작

작업을 시작하기 전에 다음 내용을 확인한다.

1. 연결된 GitHub 이슈가 존재하는지 확인한다.
2. 이슈의 목표, 범위, 완료 기준과 제외 범위를 읽는다.
3. 연결된 OpenSpec change와 관련 명세를 확인한다.
4. 요구사항이 불명확하거나 서로 충돌하면 구현 전에 명확히 한다.

새로운 작업에 연결된 이슈가 없으면 사용자에게 확인하거나 이슈를 먼저 준비한다.

### 2.2 이슈의 역할

GitHub 이슈에는 다음 내용을 유지한다.

- 작업 배경과 목표
- 대상 사용자와 User Story
- 주요 기능 범위
- 완료 기준
- 진행 상태
- 관련 OpenSpec change, 브랜치와 Pull Request 링크

상세 요구사항과 예외 시나리오는 OpenSpec에서 관리한다. 이슈에는 검토와 진행 관리에 필요한 수준으로 요약한다. 본문 말투는 11장을 따른다.

### 2.3 브랜치 규칙

- 하나의 이슈는 하나의 작업 브랜치를 기본으로 한다.
- 브랜치 이름에는 작업 유형과 이슈 번호를 포함한다.
- 기본 형식은 `<type>/<issue-number>-<short-description>`이다.
- 작업 유형은 `feature`, `fix`, `design`, `refactor`, `test`, `docs`, `chore` 중 목적에 맞는 값을 사용한다.
- 예시: `feature/12-composite-diff`, `fix/27-rename-history`, `design/1-initial-architecture`
- 브랜치에는 해당 이슈와 직접 관련된 변경만 포함한다.
- 커밋과 Pull Request에서 이슈 번호를 추적할 수 있게 한다.

## 3. OpenSpec과 SDD

### 3.1 명세 기준

- OpenSpec은 구현 대상 동작의 기준이다.
- 신규 기능과 동작 변경은 연결된 OpenSpec change에서 정의한다.
- 구현 전에 proposal, specs, design과 tasks를 읽는다.
- 사용자에게 보이는 동작은 검증 가능한 요구사항과 시나리오로 작성한다.
- 요구사항에는 정상 흐름, 경계 조건, 오류 처리와 부분 결과를 포함한다.
- 구현 과정에서 요구사항이 변경되면 OpenSpec을 함께 갱신한다.
- 구현과 명세가 다를 때는 차이를 방치하지 않고 요구사항을 확인한 뒤 일치시킨다.

### 3.2 OpenSpec 작업 순서

1. GitHub 이슈의 목표와 범위를 확인한다.
2. 관련 OpenSpec change를 생성하거나 기존 change를 선택한다.
3. proposal에서 변경 목적과 capability를 정의한다.
4. specs에서 요구사항과 시나리오를 정의한다.
5. design에서 구현 방향, 제약과 설계 결정을 정리한다.
6. tasks를 검증 가능한 구현 단위로 분해한다.
7. `npx --no -- openspec validate <change-name>`으로 명세를 검증한다.
8. 명세가 구현 가능한 상태가 된 후 코드를 작성한다.
9. 구현 완료 후 tasks와 실제 결과가 일치하는지 확인한다.
10. 검증과 리뷰가 끝난 change를 archive한다.

### 3.3 요구사항 작성 기준

- 요구사항은 사용자가 관찰할 수 있는 동작을 중심으로 작성한다.
- 각 요구사항은 하나 이상의 검증 가능한 시나리오를 포함한다.
- 시나리오는 입력 조건, 사용자 행동과 기대 결과를 명확히 표현한다.
- 내부 구현 방식은 사용자 동작에 필요한 제약이 있을 때 design에서 다룬다.
- 오류 메시지에는 문제 범위, 원인과 사용자가 취할 수 있는 다음 행동을 포함한다.
- 일부 결과를 제공할 수 있는 경우 정상 결과와 문제 범위를 함께 제공한다.

## 4. TDD

### 4.1 기본 주기

구현은 다음 주기를 따른다.

1. OpenSpec 시나리오와 완료 기준을 확인한다.
2. 기대 동작을 나타내는 실패 테스트를 작성한다.
3. 테스트를 통과시키는 최소한의 코드를 작성한다.
4. 모든 관련 테스트가 통과하는 상태에서 구조와 이름을 개선한다.
5. 중복을 제거하고 설계 원칙을 점검한다.
6. 전체 관련 테스트를 다시 실행한다.

### 4.2 테스트 기준

- 신규 기능에는 요구사항을 검증하는 테스트를 작성한다.
- 버그 수정에는 문제를 재현하는 회귀 테스트를 작성한다.
- 정상 흐름, 경계값, 오류와 취소 흐름을 검증한다.
- 테스트는 실행 순서와 외부 상태에 의존하지 않도록 작성한다.
- 테스트 이름은 조건과 기대 결과를 이해할 수 있게 작성한다.
- 구현 세부사항보다 외부 동작과 공개 계약을 검증한다.
- 테스트를 통과시키기 위해 검증을 약화하거나 의미 있는 단언을 제거하지 않는다.

## 5. 코드 품질

### 5.1 가독성

- 이름만으로 역할과 의도를 이해할 수 있게 작성한다.
- 함수와 클래스는 명확한 하나의 책임을 갖게 한다.
- 복잡한 조건과 긴 흐름은 의미 있는 단위로 분리한다.
- 숨겨진 상태 변경과 예상하기 어려운 부수 효과를 줄인다.
- 주석은 코드가 하는 일보다 결정의 이유와 제약을 설명하는 데 사용한다.
- 영리한 축약보다 읽기 쉽고 명시적인 표현을 사용한다.

### 5.2 설계 원칙

- 관심사를 분리하고 변경 이유가 다른 코드를 분리한다.
- 의존성 방향을 명확하게 유지한다.
- 외부 시스템과 프레임워크 의존성을 경계에 모은다.
- 도메인 규칙이 여러 위치에 중복되지 않게 한다.
- 안정적인 인터페이스 뒤에 변경 가능성이 큰 구현을 둔다.
- 확장 가능성이 확인된 지점에 재사용 가능한 추상화를 만든다.
- 사용하지 않는 범용화와 불필요한 계층을 추가하지 않는다.

### 5.3 객체지향 언어

Java와 같은 객체지향 언어에서는 다음 원칙을 준수한다.

- 캡슐화를 유지하고 객체의 유효한 상태를 객체 내부에서 보호한다.
- 단일 책임 원칙을 지킨다.
- 변경에 열려 있고 안정된 동작을 보호할 수 있는 구조를 사용한다.
- 하위 타입은 상위 타입의 계약을 지킨다.
- 사용자가 필요로 하는 작은 인터페이스를 제공한다.
- 구체 구현에 대한 직접 의존을 줄이고 역할과 계약에 의존한다.
- 상속 관계가 명확하지 않으면 조합을 사용한다.
- 값 객체와 불변 상태를 활용해 상태 변경 범위를 줄인다.

### 5.4 재사용성과 유지보수성

- 동일한 도메인 규칙은 한 곳에서 관리한다.
- 공통 동작을 추출할 때 호출자의 의미와 변경 방향이 같은지 확인한다.
- 모듈 간 계약을 작고 명확하게 유지한다.
- 오류를 삼키지 않고 호출자가 판단할 수 있는 정보와 함께 전달한다.
- 설정값, 경로와 환경별 값은 코드에 분산해 하드코딩하지 않는다.
- 새로운 기능이 기존 동작에 미치는 영향을 테스트로 보호한다.

## 6. 구현 및 변경 관리

- 작업 전에 현재 브랜치와 작업 트리 상태를 확인한다.
- 사용자가 만든 변경과 현재 이슈에 포함되지 않은 변경을 보존한다.
- 작은 단위로 구현하고 각 단위마다 테스트 가능한 상태를 유지한다.
- 관련 없는 리팩터링과 포맷 변경을 같은 작업에 포함하지 않는다.
- 의존성 추가는 필요성, 유지보수 상태와 영향 범위를 확인한다.
- 공개 동작을 변경하면 명세, 테스트와 사용자 문서를 함께 갱신한다.
- 실패와 부분 성공 상태를 명시적으로 표현하고 진단 가능한 정보를 제공한다.

### 6.1 React 코드 규칙

- 컴포넌트와 Hook은 같은 props와 state에 같은 JSX를 반환하는 순수한 렌더링을 유지한다.
- props, state, context와 ref 값을 직접 변경하지 않는다.
- Hook은 React 컴포넌트 또는 사용자 Hook의 최상위에서 같은 순서로 호출한다.
- 화면 계산값은 기존 상태에서 구하고 같은 의미의 값을 별도 state로 중복 저장하지 않는다.
- Effect는 Electron 구독, Monaco model, 포커스 복원처럼 외부 시스템과 동기화할 때만 사용한다.
- 사용자 동작은 이벤트 처리 함수에서 시작하고 모든 구독, model과 취소 가능한 작업은 종료 시 정리한다.
- 관련 화면 상태는 구분된 상태와 reducer로 관리하며 서로 모순되는 boolean 조합을 만들지 않는다.
- 커밋과 파일 목록의 key에는 전체 커밋 ID와 저장소 상대 경로처럼 안정적인 식별자를 사용한다.
- Git 출력, 경로와 진단 문구는 텍스트로 렌더링하고 `dangerouslySetInnerHTML`을 사용하지 않는다.
- 컴포넌트 props와 전달 자료는 읽기 전용 타입으로 선언한다.
- 컴포넌트는 PascalCase, 사용자 Hook은 `use`, 이벤트 props는 `on`, 내부 이벤트 처리 함수는 `handle` 접두사를 사용한다.
- `useMemo`, `useCallback`과 `memo`는 측정된 병목 또는 외부 계약의 안정적인 참조가 필요한 경우에 사용한다.
- 기본 HTML 컨트롤, 보이는 이름, 키보드 조작, 포커스 표시와 보조 기술 상태 알림을 제공한다.
- React 파일은 기능별로 컴포넌트, 상태, Hook, 스타일과 테스트를 함께 둔다.

### 6.2 Electron 코드 규칙

- Git, 파일 시스템, 폴더 선택과 통합 결과 계산은 main 프로세스에서 실행한다.
- renderer는 Node.js와 Electron 모듈을 사용하지 않고 preload가 공개한 typed API만 호출한다.
- preload는 사용자 동작별 함수를 공개하고 `ipcRenderer`, 임의 채널과 Electron 이벤트 객체를 노출하지 않는다.
- main은 모든 프로세스 간 요청의 자료형, 저장소 세션, 요청 세대와 발신 화면을 실행 시점에 검증한다.
- main, preload, renderer와 shared 계약의 의존성 방향은 lint와 별도 TypeScript 설정으로 보호한다.
- `contextIsolation`과 renderer sandbox를 켜고 `nodeIntegration`을 끈다.
- 로컬 앱 주소 밖의 탐색, 새 창, 권한 요청과 외부 콘텐츠 실행을 차단한다.
- lint 비활성화 주석에는 규칙 이름, 필요한 이유와 연결된 이슈를 기록하며 파일 전체 비활성화는 사용하지 않는다.

## 7. 검증

작업 완료 전 다음 항목을 확인한다.

- OpenSpec 요구사항과 구현이 일치한다.
- OpenSpec change가 형식 검증을 통과한다.
- 관련 tasks가 실제 완료 상태와 일치한다.
- 신규 테스트와 회귀 테스트가 통과한다.
- 관련 전체 테스트가 통과한다.
- lint, format, type check와 build가 필요한 범위에서 통과한다.
- 현재 이슈와 관련 없는 변경이 포함되지 않았다.
- 오류 메시지와 부분 결과가 사용자가 이해할 수 있는 정보를 제공한다.
- README와 사용자 문서가 실제 동작과 일치한다.

검증하지 못한 항목은 완료로 보고하지 않고 이유와 영향을 명시한다.

## 8. 작업 완료 및 인계

- GitHub 이슈의 완료 기준과 OpenSpec 시나리오를 기준으로 결과를 확인한다.
- 완료한 OpenSpec task를 실제 상태에 맞게 갱신한다.
- Pull Request에는 변경 목적, 주요 동작, 테스트 결과와 관련 이슈를 기록한다.
- 남은 문제, 제한 사항과 후속 작업을 명확히 남긴다.
- 사용자가 확인해야 할 사항과 재현 또는 검증 방법을 제공한다.

## 9. 커밋 규칙

### 9.1 커밋 메시지

커밋 메시지는 다음 형식을 사용한다.

```text
<type>: #<issue-number> <summary>

- <detail>
```

- `type`은 `feat`, `bug`, `docs`, `test`, `refactor`, `style`, `perf`, `build`, `ci`, `chore` 중에서 선택한다.
- 이슈 번호는 `#1` 형식으로 작성한다.
- 제목은 변경 목적을 명확하게 표현하고 끝에 마침표를 사용하지 않는다.
- 상세 내용은 복잡한 변경이나 추가 설명이 필요한 경우에만 작성한다.
- 상세 내용에는 변경 사항과 필요한 이유를 항목별로 작성한다.
- 제목과 상세 내용의 말투는 11장을 따른다.
- 커밋 전 스테이징된 전체 diff와 관련 검증 결과를 확인한다.

커밋 템플릿은 `.github/COMMIT_TEMPLATE.md`를 사용하고, 커밋 작업은 `.codex/skills/prettifer-commit/`의 절차를 따른다.

## 10. Pull Request 규칙

### 10.1 PR 본문

Pull Request 본문에는 다음 내용을 기록한다.

- 변경 요약과 영향
- 연결된 GitHub 이슈
- 관련 OpenSpec change와 요구사항
- 코드 품질 자체 리뷰 결과
- 테스트와 검증 결과
- 남은 위험과 후속 작업

본문 말투는 11장을 따른다.

PR 템플릿은 `.github/pull_request_template.md`를 사용하고, PR 작업은 `.codex/skills/prettifer-pr/`의 절차를 따른다.

### 10.2 자체 리뷰 기준

- 이슈 범위와 OpenSpec 요구사항이 diff와 일치하는지 확인한다.
- 이름, 함수, 클래스와 모듈의 역할이 명확한지 확인한다.
- 책임 분리, 의존성 방향과 캡슐화가 적절한지 확인한다.
- 중복된 도메인 규칙과 불필요한 결합이 없는지 확인한다.
- 재사용성과 유지보수성을 해치는 구조가 없는지 확인한다.
- 사용 언어의 기본 설계 원칙을 준수하는지 확인한다.
- 정상 흐름, 경계 조건, 오류와 부분 성공 상태가 테스트되는지 확인한다.
- 문서 변경은 요구사항 모순, 용어 일관성, 검증 가능성과 추적성을 확인한다.

자체 리뷰 결과는 확인 여부만 표시하지 않고 실제 발견 사항, 수정 결과와 남은 위험을 포함한다.

### 10.3 병합 및 이슈 종료

- 해결되지 않은 중요한 리뷰 발견 사항이 있으면 병합하지 않는다.
- 필요한 검증이 실패하면 병합하지 않는다.
- 사용자가 병합을 요청한 경우에도 PR이 이슈 범위와 일치하고 검증을 통과한 경우에만 병합한다.
- 사용자가 이슈 종료를 요청한 경우 관련 PR이 병합된 후 닫는다.
- 자동 종료 문구를 사용한 경우 병합 후 이슈 상태를 확인한다.

## 11. 산출물 말투

커밋 메시지, 이슈와 Pull Request는 사람이 읽는 글이므로 다음 말투를 사용한다.

- 커밋 제목은 명사형으로 끝낸다. 예: `feat: #19 diff 좌우·인라인 보기 전환 추가`
- 커밋 상세 내용, 이슈 본문과 PR 본문은 `~합니다`로 작성한다.
- 겪은 상황과 판단 근거를 그대로 쓰고, 정보를 담지 않은 감탄이나 홍보 문구를 넣지 않는다.
- 다른 문서의 문장을 인용할 때는 원문의 말투를 그대로 유지한다.
- 릴리스 노트는 `CHANGELOG.md` 머리말의 작성 지침을 따른다.

이 규칙은 산출물에만 적용한다. 이 문서와 OpenSpec 명세는 규범 문서이므로 현재의 서술체와
`~해야 한다(SHALL)` 표현을 유지한다.
