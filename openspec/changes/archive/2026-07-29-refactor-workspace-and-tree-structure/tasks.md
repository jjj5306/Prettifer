## 1. 검증 기준선

- [x] 1.1 리팩터링 대상 4곳을 덮는 기존 테스트를 확인하고 실행해 통과 기준선을 잡는다
- [x] 1.2 설정 탐색 패턴이 잡아야 하는 키와 잡지 않아야 하는 키를 검증하는 테스트를 작성한다
- [x] 1.3 같은 디렉터리를 여러 파일이 공유할 때 중복 없이 한 번만 만들어지고 표시 순서가 유지되는지 검증하는 테스트를 작성한다

## 2. 설정 적용과 준비 흐름 분리

- [x] 2.1 설정 적용 메서드가 적용한 키를 담은 값을 돌려주도록 바꾼다
- [x] 2.2 전체 작업 트리가 필요한지 판단하는 이름 있는 질의를 분리하고 판단 근거를 주석으로 남긴다
- [x] 2.3 준비 흐름을 전체 작업 트리 준비와 선택 경로 준비로 나눈다
- [x] 2.4 두 준비 동작이 공유하는 checkout 실행을 한 곳으로 모은다
- [x] 2.5 호출부가 질의 결과로 한 번만 분기하도록 정리한다

## 3. 설정 키 목록 자료화

- [x] 3.1 고정 키와 이름이 들어가는 키를 각각 자료로 선언한다
- [x] 3.2 선언한 자료에서 Git이 해석할 수 있는 탐색 패턴을 생성한다
- [x] 3.3 생성한 패턴이 리팩터링 전 패턴과 같은 키 집합을 잡는지 확인한다

## 4. 트리 구성 자료 구조 단일화

- [x] 4.1 자식 배열과 디렉터리 맵을 삽입 순서를 유지하는 단일 맵으로 합친다
- [x] 4.2 결과 트리 노드를 그 맵에서 파생하도록 바꾼다
- [x] 4.3 디렉터리 중복 확인과 자식 추가가 같은 자료를 보는지 확인한다

## 5. 전체 검증

- [x] 5.1 lint, typecheck와 전체 유닛 테스트를 실행한다
- [x] 5.2 실제 앱 e2e를 실행해 동작 변경이 없음을 확인한다
- [x] 5.3 OpenSpec strict 검증을 실행한다
