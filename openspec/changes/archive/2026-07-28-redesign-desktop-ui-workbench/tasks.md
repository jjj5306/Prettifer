## 1. 현재 동작과 테스트 기준선

- [x] 1.1 첫 실행, 저장소 열기, 비교 범위, 커밋 선택, 계산, 결과와 오류 흐름을 현재 테스트와 실제 앱에서 확인한다
- [x] 1.2 renderer 콘솔 오류와 처리되지 않은 Promise를 수집하는 Playwright 검증 경계를 준비한다
- [x] 1.3 영어 문구, 디자인 토큰과 주요 화면 상태를 검증하는 실패 테스트를 작성한다

## 2. 디자인 토큰과 작업대

- [x] 2.1 graphite 팔레트, Git 상태, 간격, 테두리와 포커스 토큰을 renderer 최상위 스타일에 정의한다
- [x] 2.2 저장소, 비교 범위, 커밋, 변경 파일, diff와 작업 상태를 연속해서 탐색할 수 있는 앱 작업대를 구현한다
- [x] 2.3 사용자에게 보이는 기본 UI 문구와 접근 가능한 이름을 영어로 통일한다
- [x] 2.4 로딩, 빈 결과, 취소, 오래된 범위와 오류 상태에 문제 범위 및 복구 행동을 표시한다
- [x] 2.5 1280×720, 1920×1080, 200% 확대, forced-colors와 reduced-motion 화면 규칙을 구현한다

## 3. 변경 파일 Tree/List 보기

- [x] 3.1 Tree View와 List View 전환, 선택 유지와 키보드 동작을 검증하는 실패 테스트를 작성한다
- [x] 3.2 원본 저장소 상대 경로를 유지하는 변경 파일 계층 계산을 순수 함수로 구현하고 경로 경계값을 테스트한다
- [x] 3.3 이름과 선택 상태가 있는 Tree/List view toggle을 `Changed Files` 패널에 구현한다
- [x] 3.4 Tree View에서 폴더 계층, 파일 상태와 선택 파일을 표시한다
- [x] 3.5 List View에서 전체 상대 경로, 파일 상태와 선택 파일을 표시한다
- [x] 3.6 보기 전환 뒤 선택 파일과 현재 diff가 유지되고 새 Git 또는 계산 요청이 발생하지 않음을 검증한다
- [x] 3.7 빈 결과, 긴 경로와 마크업 형태의 경로를 두 보기에서 안전하게 표시한다

## 4. 상태와 Electron 경계

- [x] 4.1 저장소 전환, 비교 범위 변경과 계산 취소 뒤 이전 응답이 화면에 적용되지 않는 회귀 테스트를 보강한다
- [x] 4.2 현재 reducer와 selector에서 계산 가능한 화면 값을 구하고 중복된 UI 상태를 제거한다
- [x] 4.3 renderer가 typed preload API만 사용하고 Electron 및 Node.js 모듈에 접근하지 않음을 검증한다
- [x] 4.4 성공, 실패와 취소 흐름에서 사용자 branch, HEAD와 작업 트리 상태가 보존됨을 검증한다

## 5. 실제 앱과 시각 검증

- [x] 5.1 Playwright Electron으로 저장소 열기부터 커밋 선택, 계산, 파일 선택과 diff 검토까지 실행한다
- [x] 5.2 실제 앱에서 Tree View와 List View를 전환하고 선택 파일 및 diff 유지 상태를 검증한다
- [x] 5.3 1280×720과 1920×1080의 두 보기 및 200% 확대 화면을 캡처해 목업의 색상과 UI 톤을 검토한다
- [x] 5.4 오류 복구, 계산 취소, 다른 저장소 전환과 앱 재실행 흐름을 검증한다
- [x] 5.5 Playwright 실행에서 renderer 콘솔 오류와 처리되지 않은 Promise가 없음을 확인한다

## 6. 정리와 전체 검증

- [x] 6.1 사용하지 않는 이전 UI 컴포넌트, 스타일, selector, action과 test fixture를 제거한다
- [x] 6.2 중복된 상태 표현, 진단 변환과 파일 상태 표현을 제거하고 기능 경계를 다시 검토한다
- [x] 6.3 OpenSpec 요구사항별 테스트 추적성과 실제 완료 상태를 확인한다
- [x] 6.4 format, lint, typecheck, 전체 테스트와 OpenSpec strict 검증을 실행한다
- [x] 6.5 Windows package와 ZIP을 생성하고 운영 패키지 smoke 테스트를 실행한다
- [x] 6.6 README의 두 목업과 실제 앱 화면 및 사용 흐름을 일치시킨다

## 7. 실제 사용 피드백 보완

- [x] 7.1 커밋 카드 본문 클릭이 상세 보기만 바꾸고 선택 수를 갱신하지 않는 문제를 회귀 테스트로 재현한다
- [x] 7.2 Hanken Grotesk와 Geist 글꼴을 앱 패키지에 포함하고 UI와 코드 글꼴 토큰에 적용한다
- [x] 7.3 저장소, Commit History, Changed Files와 Diff Review로 이동하는 좌측 activity rail을 구현하고 접근성을 검증한다
- [x] 7.4 선택 가능한 커밋 카드 전체를 다중 선택 동작으로 연결하고 선택 수 및 결과 만들기 상태를 검증한다
- [x] 7.5 실제 Electron 앱에서 카드 두 개 선택, activity rail, Tree/List 토글과 diff 흐름을 Playwright로 검증하고 목업과 다시 비교한다
- [x] 7.6 lint, typecheck, 전체 테스트, OpenSpec strict 검증과 Windows 패키지를 다시 실행한다

## 8. 계산 성능과 검토 공간 보완

- [x] 8.1 대형 저장소의 전체 checkout 지연, 독립 적용할 수 없는 중간 커밋과 activity rail 선택 실패를 회귀 테스트로 재현한다
- [x] 8.2 선택 커밋의 변경 경로만 sparse checkout하고 성공, 실패와 취소 뒤 임시 worktree를 완전히 정리한다
- [x] 8.3 독립 적용할 수 없는 커밋을 식별하고 필요한 이전 커밋 선택 안내를 안전하게 표시한다
- [x] 8.4 activity rail을 앱 전체 높이의 왼쪽 열로 이동하고 실행한 메뉴의 선택 상태를 유지한다
- [x] 8.5 기본 UI 크기를 한 단계 키우고 커밋 타임라인을 압축해 diff 영역의 높이와 코드 가독성을 높인다
- [x] 8.6 lint, typecheck, 전체 테스트, OpenSpec strict 검증과 Windows 패키지를 다시 실행한다

## 9. Diff 중심 화면 밀도 보완

- [x] 9.1 Commit History 명칭, 오래된 커밋부터 최신 커밋 순서와 1280×720 diff 높이 70% 기준을 회귀 테스트로 고정한다
- [x] 9.2 저장소 범위, Commit History와 Selected Result를 한 줄 중심의 작업 바로 압축한다
- [x] 9.3 Commit History를 가장 오래된 커밋이 왼쪽, 가장 최신 커밋이 오른쪽에 오도록 표시한다
- [x] 9.4 실제 Electron 앱 1280×720에서 변경 파일과 diff 검토 영역 높이 70% 이상 및 주요 동작을 검증한다
- [x] 9.5 lint, typecheck, 전체 테스트, OpenSpec strict 검증과 Windows 패키지를 다시 실행한다

## 10. Diff 높이, Tree View와 추가 파일 보기 보완

- [x] 10.1 diff 편집기 높이, Tree View 폴더 접기와 추가 파일 표시를 회귀 테스트로 재현한다
- [x] 10.2 diff 편집기가 diff 검토 영역의 남는 높이를 모두 채우도록 패널 행 배치를 수정한다
- [x] 10.3 Tree View 폴더 행을 접고 펼 수 있는 컨트롤로 만들고 펼침 상태를 보조 기술에 제공한다
- [x] 10.4 Tree View 계층선을 각 행의 가로 연결선으로 끝내고 마지막 행 아래로 이어지지 않게 한다
- [x] 10.5 새로 추가된 파일을 기준 내용 없이 전체 추가 내용으로 표시한다
- [x] 10.6 실제 Electron 앱에서 diff 높이, 폴더 접기와 추가 파일 보기를 검증한다
- [x] 10.7 lint, typecheck, 전체 테스트, OpenSpec strict 검증을 다시 실행한다

## 11. 깊은 경로 들여쓰기와 작업 바 정렬

- [x] 11.1 깊은 경로 들여쓰기 누적과 작업 바 높이 불일치를 회귀 테스트로 재현한다
- [x] 11.2 하위 폴더가 하나뿐인 경로를 한 행으로 합쳐 Tree View 들여쓰기 누적을 없앤다
- [x] 11.3 앱 바, 저장소 범위, Commit History와 Selected Result에 공용 높이 토큰을 적용한다
- [x] 11.4 커밋 카드를 한 줄로 압축하고 폭 부족 시 작성자 이름부터 줄어들게 한다
- [x] 11.5 실제 Electron 앱 1280×720에서 네 작업 바 높이 일치와 diff 높이 70% 기준을 검증한다
- [x] 11.6 lint, typecheck, 전체 테스트와 OpenSpec strict 검증을 다시 실행한다

## 12. 기존 데스크톱 명세 동기화

- [x] 12.1 Commit History의 시간순 표시를 `desktop-repository-navigation`에 반영한다
- [x] 12.2 추가 파일의 한 화면 검토 동작을 `desktop-composite-diff-review`에 반영한다
- [x] 12.3 변경된 기존 capability와 메인 스펙의 일치를 검증한다

## 13. 병합 전 유지보수성 자체 리뷰

- [x] 13.1 원본 저장소의 config와 다른 worktree 등록을 변경하는 임시 작업 공간
  회귀를 테스트로 재현한다
- [x] 13.2 원본과 분리된 임시 clone에 파일 내용 계산용 로컬 설정만 전달하고
  성공, 실패와 취소 뒤 이번 요청의 디렉터리만 정리한다
- [x] 13.3 최종 텍스트를 Git index에서 읽어 Windows 줄바꿈 변환과 무관한 결과를
  제공한다
- [x] 13.4 실제 병합되지 않은 항목이 있는 적용 실패만 커밋 충돌로 분류하고 다른
  Git 실패의 원래 진단을 유지한다
- [x] 13.5 선택 커밋의 변경 경로와 파일 내용 조회 동시성을 제한하고 List View의
  파일 트리 계산을 생략하며 Tree View 경로 탐색을 디렉터리 색인으로 처리한다
- [x] 13.6 사용할 수 없는 activity rail 항목이 현재 메뉴로 남지 않는 접근성
  회귀 테스트를 작성하고 상태를 수정한다
- [x] 13.7 추가, 수정, 삭제와 바이너리 파일 자료형을 유효한 내용 조합으로 제한하고
  화면 경계 검증 테스트를 보강한다
- [x] 13.8 Monaco 준비 과정의 각 실패 지점에서 생성된 editor, model과 장식을
  모두 해제하는 회귀 테스트와 정리 동작을 구현한다
- [x] 13.9 커밋 카드의 탐색 상태와 카드 안의 식별 정보를 명세 및 테스트와
  일치시킨다
- [x] 13.10 관련 메인 스펙, 검증 자료와 README를 최종 동작에 맞추고 format,
  lint, typecheck, 전체 테스트, E2E, package와 OpenSpec strict 검증을 실행한다
