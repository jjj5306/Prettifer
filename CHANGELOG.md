# 변경 기록

Prettifer를 쓰는 사람이 읽는 글입니다. 어떤 코드가 바뀌었는지가 아니라 **뭐가 달라졌고 그래서 뭐가
편해지는지**를 씁니다.

- 읽는 사람이 이 프로그램을 만든 사람이라고 가정하지 않습니다. 내부 이슈 번호, 브랜치 이름, 소스
  파일 경로는 쓰지 않습니다.
- 화면에 영어로 뜨는 이름은 괄호로 같이 적습니다. 예: 선택 결과(Selected Result).
- 왜 그렇게 만들었는지 설명이 필요하면 개발 노트로 덧붙입니다.

배포 워크플로가 해당 버전 절을 릴리스 노트에 그대로 넣습니다. 절이 없으면 게시가 실패합니다.

## v0.3.0

이번 버전은 **"코드를 읽다가 흐름이 끊기는 순간"** 을 없애는 데 집중했습니다.

diff를 보다가 처음 보는 클래스 이름이 나오면 어떻게 하셨나요. 아마 IDE를 따로 켜서 그 이름을
검색하고, 찾아 읽고, 다시 Prettifer로 돌아왔을 겁니다. 그 왕복이 이제 없습니다.

### 이제 이런 걸 할 수 있습니다

**이름 하나만 눌러서 코드를 따라가기**

검토 중인 코드에서 클래스나 함수 이름을 `Ctrl+클릭` 해보세요. 그 이름이 선언된 자리로 바로
갑니다. `F12`도 같고, `Shift+F12`를 누르면 그 이름을 쓰는 자리가 전부 목록으로 나옵니다. 목록에서
하나 고르면 그리로 가고, `Back` 버튼이면 원래 보던 자리로 돌아옵니다.

Java, C/C++, TypeScript, JavaScript에서 됩니다.

**내가 고른 커밋 밖에 있어도 찾아옵니다**

이게 핵심입니다. 찾는 범위가 **저장소 전체**입니다. 내가 고른 커밋이 손도 안 댄 파일에 그 선언이
있어도 찾아서 열어 줍니다. 이 경우 화면에 "선택 결과에 없는 파일"이라고 표시하고 비교 기준 시점의
내용을 보여 주니, 지금 보는 게 선택 결과인지 아닌지 헷갈릴 일은 없습니다.

**클래스인지 생성자인지 알려 줍니다**

`Foo`를 눌렀는데 클래스 선언, 생성자 두 개가 우르르 나오면 뭘 골라야 할지 모르겠죠. 이제 목록마다
형(type), 생성자(ctor), 함수·메서드(func), 필드(field), 변수(var)로 표시가 붙습니다.

거기다 **누른 자리를 보고 알아서 골라 줍니다.** `new Foo(...)`처럼 객체 만드는 자리에서 눌렀으면
생성자를, 그 밖에서 눌렀으면 클래스 선언을 먼저 봅니다. 후보가 하나면 묻지 않고 바로 이동하고,
같은 이름이 진짜로 여럿일 때만 목록으로 물어봅니다.

> **개발 노트.** 여기서 목록을 다 보여 주는 쪽이 안전해 보이지만, `get` 같은 이름을 한번
> 눌러보면 왜 안 그랬는지 아실 겁니다. 저장소 곳곳의 지역 변수가 수십 개 섞여 나와서 정작 찾던
> 메서드가 묻힙니다. 그래서 가장 그럴듯한 종류만 남깁니다.

**따라갈 수 있는 이름인지 미리 보기**

`Ctrl`을 누른 채 이름 위에 마우스를 올리면 밑줄이 생깁니다. 눌러도 되는 이름인지 눌러보기 전에
알 수 있습니다.

**diff를 위아래로 이어서 읽기**

좌우로 나란히 보는 방식만 있었는데, 삭제된 줄과 추가된 줄을 위아래로 이어 보는 방식이 생겼습니다.
검토 화면 오른쪽 위 버튼으로 바꿉니다. 한 줄이 긴 코드나 좁은 화면에서는 좌우로 쪼개면 양쪽이 다
잘려서 가로 스크롤만 하게 되는데, 그럴 때 쓰세요. **바꿔도 보고 있던 위치는 그대로입니다.**

> **개발 노트.** 창이 좁아지면 알아서 세로로 바꿔줄까 고민했는데, 넣지 않았습니다. 창 크기 조절만
> 했는데 내가 고른 보기가 뒤집히면 황당하니까요. 버튼이 바로 옆에 있으니 필요할 때 직접 누르는
> 편이 낫다고 봤습니다.

**Java와 C/C++에 색이 들어갑니다**

이 두 언어만 색 없는 검은 글씨였습니다. 이제 다른 언어들처럼 구문 강조가 됩니다.

### 이건 좀 나아졌습니다

- 검토 화면 제목 줄이 **지금 보고 있는 파일 경로**를 알려 줍니다. 이름 따라 파일을 오가다 보면
  내가 어느 파일에 있는지 놓치는데, 이제 위만 보면 됩니다.
- 선언으로 이동하면 커서가 그 줄의 **이름 위에** 놓이고 도착한 줄에 표시가 남습니다. 예전처럼 줄
  맨 앞에서 멈추면 "그래서 이 줄에서 뭘 보라는 거지" 싶었는데, 이제 눈에 바로 들어옵니다.

### 거슬렸던 것

- **이름을 따라가려고 하면 스크롤이 맨 위로 튀던 문제를 고쳤습니다.** 500줄짜리 파일 중간을 보다가
  참조 목록 한번 열면 파일 맨 앞으로 돌아가 있었죠. 결과는 아래에 잘 나오는데 정작 보던 자리를
  잃는, 제일 짜증나는 종류의 버그였습니다.

## v0.2.0

작지만 매번 걸리던 두 가지를 정리했습니다.

### 이제 이런 걸 할 수 있습니다

- **저장소를 바로 열고 시작합니다.** 실행할 때 저장소 경로를 같이 주면 폴더 선택 창을 건너뛰고 그
  저장소로 시작합니다. 매번 같은 폴더를 찾아 들어가던 걸 안 해도 됩니다. 경로가 저장소가 아니면
  뭐가 잘못됐는지 화면에 알려 줍니다.

### 이건 좀 나아졌습니다

- **트리 보기에서 각 줄이 자기 이름만큼만 차지합니다.** 전에는 창을 넓히면 줄도 같이 화면 끝까지
  늘어나서, 폴더 구조가 아니라 긴 막대가 쌓인 것처럼 보였습니다. 이제 넓혀도 이름 길이만큼만
  차지해서 어디가 어느 폴더인지 눈에 들어옵니다.

## v0.1.0

첫 Windows 배포입니다.

Git 이력에서 **서로 떨어져 있는 커밋만 골라**, 그 변경만 반영된 최종 파일 상태와 통합 diff 하나를
만들어 검토할 수 있습니다. 관련 없는 커밋이 섞인 브랜치에서 "이 작업만" 떼어내 보고 싶을 때 쓰는
도구입니다.

- 같은 파일을 여러 번 고친 커밋을 함께 골라도 최종 상태 하나로 합쳐 보여 줍니다.
- 텍스트 파일의 추가·수정·삭제를 구분하고, 바이너리 파일은 바이너리라고 표시합니다.
- merge 커밋도 기준으로 삼을 부모를 고르면 결과에 넣을 수 있습니다.
- 적용할 수 없는 파일이 있으면 그 파일만 문제로 표시하고 나머지는 그대로 검토할 수 있습니다.
- **작업 중인 내용은 건드리지 않습니다.** 계산하는 동안에도 내 branch, HEAD, 커밋 안 한 변경이 다
  그대로입니다. 이건 타협하지 않았습니다.
