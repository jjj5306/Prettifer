# 변경 기록

- 버전별 사용자 영향만 기록
- 분류: `추가`, `변경`, `수정`, `보안`
- 한 항목에 한 가지 변경만 작성
- 내부 이슈 번호, 브랜치 이름, 소스 경로 제외
- 화면 명칭은 제품에 표시되는 이름 사용

배포 워크플로는 해당 버전 절을 릴리스 노트에 포함한다. 버전 절이 없으면 게시 실패.

## 미출시

### 추가

- Activity Rail에 `About Prettifer` 추가. 실행 중인 버전과 저장소 주소를 확인합니다

### 변경

- 선택 파일의 커밋 이력을 `Changed Files` 머리글의 `File History`로 엽니다
- 이력과 커밋별 변경을 diff 검토 영역에 표시. 이력을 보는 동안에도 변경 파일 목록이 남습니다
- `Escape`는 커밋 변경에서 이력으로, 다시 누르면 통합 diff로 한 단계씩 복귀
- Activity Rail에서 `File History`와 `Group Rules` 항목 제거. 두 동작은 `Changed Files`에서 시작합니다
- diff 비교 화면 제목을 `Differentia Codicis`로 표시
- `Config View`와 `Full Tree` 토글에 서로 다른 아이콘 적용
- 긴 경로의 변경 파일 행에서 파일 이름과 그 폴더를 줄이지 않고 위 폴더만 줄여 표시
- 변경 파일 목록의 기본 폭을 머리글 컨트롤이 한 줄에 들어오는 폭으로 조정

### 수정

- 파일 이력을 보고 통합 diff로 돌아올 때 스크롤과 커서 위치가 복원되지 않던 문제

## v0.5.0

### 추가

- 이름이 바뀐 파일을 하나의 비교로 표시. 삭제와 추가 두 줄로 나뉘지 않습니다
- 이름 변경 파일에 이전 경로와 Git이 맞춘 일치 비율 표시
- 변경 파일 목록에서 이름 변경을 추가, 수정, 삭제와 구분해 표시
- Full Tree는 이름이 바뀐 파일을 현재 경로에만 표시
- 이름 변경으로 판정되지 않은 파일은 이전처럼 추가와 삭제로 표시
- Commit History에 불러온 커밋을 첫 페이지 상태로 되돌리는 초기화 추가
- Commit History에 커밋 선택을 한 번에 비우는 초기화 추가
- 두 초기화는 서로의 상태를 건드리지 않음. 목록을 줄여도 선택은 남습니다
- 초기화 뒤에도 `Load 100 older commits`로 다시 불러오기

### 변경

- merge 커밋의 기준 부모를 순서 번호 대신 어느 쪽인지와 커밋 제목으로 표시
- 통합 결과에 표시하는 기준 부모도 같은 문구 사용
- 두 초기화를 Commit History 제목 줄의 한 자리로 모으고 아이콘으로 표시
- 마우스를 올리면 그 초기화가 무엇을 되돌리고 무엇을 남기는지 설명 표시
- 되돌릴 것이 없는 초기화는 화면에 두지 않는 규칙 유지
- `Load 100 older commits`는 커밋 목록 옆에 유지

### 수정

- 폭이 좁을 때 merge 커밋 행의 정보가 겹치던 문제 수정

## v0.4.0

### 추가

- `Changed Files`에 Config View 추가. 직접 정한 경로 규칙으로 변경 파일을 묶어 봅니다
- 경로 규칙을 추가, 수정, 삭제하고 보이는 순서를 바꾸기
- 규칙은 저장소별로 따로 기억하고 앱을 다시 켜도 그대로 복원
- 어떤 규칙에도 걸리지 않은 파일은 별도 그룹에 남겨 하나도 빠뜨리지 않음
- `Changed Files`에 Full Tree 추가. 저장소 전체 구조 안에서 변경 위치를 봅니다
- Full Tree에서 변경되지 않은 파일을 골라 비교 기준 시점 내용으로 읽기
- 변경이 들어 있는 폴더는 접혀 있어도 표시
- activity rail에 Group Rules 항목 추가. 어느 보기에서든 규칙 편집으로 바로 이동

### 변경

- 커밋 카드의 빈 영역을 클릭해 선택 상태 전환
- 체크 상자, 병합 부모 선택기, 링크의 기존 동작 유지
- activity rail이 가리키는 검토 영역을 화면에 표시. 마우스로 눌러도 어디로 갔는지 보입니다
- 변경 파일 트리의 행을 평평하게 표시하고 폴더 계층선 제거
- 트리 행이 패널 너비를 채우고, 긴 이름은 경계에서 잘림. 트리가 옆으로 밀리지 않습니다
- 트리 들여쓰기와 행 높이를 줄여 한 화면에 더 많은 경로 표시
- Full Tree에서 하위가 폴더 하나뿐인 경로를 한 행으로 합침

### 보안

- `brace-expansion` 보안 업데이트
- `fast-uri` 보안 업데이트

## v0.3.0

### 추가

- `Ctrl+클릭` 또는 `F12`로 심볼 정의 이동
- `Shift+F12`로 심볼 참조 목록 표시
- `Back`으로 이전 위치 복귀
- Java, C/C++, TypeScript, JavaScript 심볼 탐색
- 저장소 전체에서 정의와 참조 검색
- 선택 결과에 없는 파일을 비교 기준 시점의 읽기 전용 내용으로 표시
- 심볼 후보에 형, 생성자, 함수·메서드, 필드, 변수 구분 표시
- 생성 호출에서는 생성자, 그 외 위치에서는 형 선언 우선 선택
- `Ctrl`을 누른 동안 이동 가능한 심볼에 밑줄 표시
- 좌우 보기와 인라인 보기 전환
- Java와 C/C++ 구문 강조
- 검토 화면에 현재 파일 경로 표시

### 변경

- diff 보기 전환 후 스크롤 위치 유지
- 정의 이동 시 심볼 위치에 커서와 도착 표시 배치

### 수정

- 심볼 탐색 시작 시 diff 스크롤이 상단으로 이동하던 문제 수정

## v0.2.0

### 추가

- 실행 인자로 받은 저장소 경로 열기
- 유효하지 않은 저장소 경로 진단 표시

### 변경

- 트리 보기의 각 행을 파일·폴더 이름 너비에 맞춤

## v0.1.0

### 추가

- 비연속 커밋 선택과 통합 diff 생성
- 같은 파일을 여러 번 변경한 선택의 최종 상태 합성
- 텍스트 파일 추가·수정·삭제 및 바이너리 파일 구분
- 병합 커밋의 기준 부모 선택
- 적용 실패 파일을 분리한 부분 결과
- 계산 중 브랜치, HEAD, 작업 트리와 Git 메타데이터 보존
